{
  "name": "bugit",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "api": "cd api && npm run start:dev",
    "web": "cd web && npm run dev",
    "dev": "concurrently \"npm run api\" \"npm run web\"",
    "install:all": "npm i && cd api && npm i && cd ../cli && npm i && cd ../web && npm i"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
